// Expression produce a value
// Statement does not produce a value

// Expression
56 + 97; // here 56 and 97 are two values which will converted into single value

26 * 45 > 54 + 77; // All values are defined in one value either if its True or False ?
// All values are calculate and comes expression  in single value.

// so expression will generate a value, which can be assign to variable.

let output = 26 * 45 > 54 + 77;// this is retreated has a statement

console.log("hey guys pls complete this assiginment");

// function declaration
function doWelcome(username) {
    console.log(`welcome ${username}`);
}
doWelcome("shami");